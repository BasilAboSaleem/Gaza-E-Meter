<!doctype html>
<html lang="ar" dir="rtl">
<%- include('../../partials/head') %>

<body class="rtl">

<div id="loading">
    <div class="loader simple-loader">
        <div class="loader-body"></div>
    </div>
</div>

<%- include('../../partials/sidebar') %>

<main class="main-content">

<div class="position-relative iq-banner">

    <%- include('../../partials/navbar') %>

    <!-- ===== Modern Header Banner ===== -->
    <div class="iq-navbar-header" style="height:220px;">
        <div class="container-fluid iq-container">

            <div class="row">
                <div class="col-md-12">

                    <div class="card bg-primary bg-gradient shadow-none rounded-4 mb-0">
                        <div class="card-body">

                            <div class="d-flex justify-content-between align-items-center flex-wrap">

                                <div>
                                    <h2 class="fw-bold text-white mb-2">
                                        مرحباً، <%= subscriber.fullName %> 👋
                                    </h2>

                                    <p class="mb-0 text-white-75">
                                        رقم الاشتراك:
                                        <span class="fw-bold text-white">
                                            <%= subscriber._id.toString().slice(-6).toUpperCase() %>
                                        </span>
                                    </p>
                                </div>

                                <div class="text-md-end mt-3 mt-md-0">
                                    <p class="text-white-75 mb-1">المنطقة</p>
                                    <h5 class="text-white fw-bold mb-0">
                                        <%= subscriber.primaryArea ? subscriber.primaryArea.name : 'غير محدد' %>
                                    </h5>
                                </div>

                            </div>

                        </div>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>

<!-- ===== Content ===== -->
<div class="container-fluid content-inner mt-n5 py-0">
    <div class="row">

        <!-- ===== Stat Cards ===== -->

        <div class="col-md-4">
            <div class="card card-block card-stretch card-height rounded-4 shadow-sm">
                <div class="card-body">
                    <div class="d-flex align-items-center justify-content-between">
                        <div>
                            <span class="text-muted">المستحقات الحالية</span>
                            <h3 class="fw-bold text-danger mt-2">
                                <%= totalDues.toLocaleString() %> ₪
                            </h3>
                        </div>
                        <div class="bg-soft-danger p-3 rounded">
                            <i class="ri-money-dollar-circle-line text-danger fs-4"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card card-block card-stretch card-height rounded-4 shadow-sm">
                <div class="card-body">
                    <div class="d-flex align-items-center justify-content-between">
                        <div>
                            <span class="text-muted">آخر قراءة</span>
                            <h3 class="fw-bold text-warning mt-2">
                                <%= lastReading ? lastReading.value : 0 %> KW
                            </h3>
                            <small class="text-muted">
                                <%= lastReading ? new Date(lastReading.readingDate).toLocaleDateString('ar-EG') : '-' %>
                            </small>
                        </div>
                        <div class="bg-soft-warning p-3 rounded">
                            <i class="ri-flashlight-line text-warning fs-4"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card card-block card-stretch card-height rounded-4 shadow-sm">
                <div class="card-body">
                    <div class="d-flex align-items-center justify-content-between">
                        <div>
                            <span class="text-muted">حالة الاشتراك</span>
                            <h3 class="fw-bold mt-2 <%= subscriber.isActive ? 'text-success' : 'text-secondary' %>">
                                <%= subscriber.isActive ? 'نشط' : 'غير نشط' %>
                            </h3>
                        </div>
                        <div class="<%= subscriber.isActive ? 'bg-soft-success' : 'bg-soft-secondary' %> p-3 rounded">
                            <i class="ri-checkbox-circle-line fs-4 <%= subscriber.isActive ? 'text-success' : 'text-secondary' %>"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ===== Recent Invoices ===== -->

        <div class="col-12">
            <div class="card rounded-4 shadow-sm">
                <div class="card-header d-flex justify-content-between align-items-center bg-white">
                    <h5 class="mb-0 fw-bold text-primary">
                        أحدث الفواتير
                    </h5>
                    <a href="/subscriber/invoices" class="btn btn-sm btn-primary rounded-pill">
                        عرض الكل
                    </a>
                </div>

                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table align-middle mb-0 table-striped">
                            <thead class="bg-soft-light text-primary">
                                <tr>
                                    <th>رقم</th>
                                    <th>التاريخ</th>
                                    <th>الاستهلاك</th>
                                    <th>المبلغ</th>
                                    <th>المتبقي</th>
                                    <th>الحالة</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% recentInvoices.forEach(invoice=> { %>
                                    <tr>
                                        <td class="fw-bold">#<%= invoice.invoiceNumber %></td>
                                        <td><%= new Date(invoice.issueDate).toLocaleDateString('ar-EG') %></td>
                                        <td><%= invoice.consumption %> KW</td>
                                        <td class="fw-bold"><%= invoice.totalAmount %> ₪</td>
                                        <td class="text-danger fw-bold"><%= invoice.remainingAmount %> ₪</td>
                                        <td>
                                            <% if (invoice.status==='PAID') { %>
                                                <span class="badge bg-soft-success text-success rounded-pill">
                                                    مدفوعة
                                                </span>
                                            <% } else if (invoice.status==='PARTIALLY_PAID') { %>
                                                <span class="badge bg-soft-warning text-warning rounded-pill">
                                                    جزئية
                                                </span>
                                            <% } else { %>
                                                <span class="badge bg-soft-danger text-danger rounded-pill">
                                                    غير مدفوعة
                                                </span>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>

                                <% if (recentInvoices.length===0) { %>
                                    <tr>
                                        <td colspan="6" class="text-center py-4 text-muted">
                                            لا توجد فواتير حديثة
                                        </td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
        </div>

    </div>
</div>

<%- include('../../partials/footer') %>

</main>

<%- include('../../partials/scripts') %>

</body>
</html>
